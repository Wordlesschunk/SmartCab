{% extends 'base.html.twig' %}

{% block title %}Edit Cabinet{% endblock %}

{% block body %}
    <h1>Edit Cabinet</h1>

    {{ include('cabinet/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_cabinet_index') }}"><button class="btn btn-info mt-3">Back to list</button></a>

    {{ include('cabinet/_delete_form.html.twig') }}
{% endblock %}
